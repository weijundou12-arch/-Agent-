0) 依赖与启动文件
pyproject.toml
[project]
name = "enterprise-med-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "jsonschema>=4.21",
]

[tool.uvicorn]
factory = false
